#!/bin/bash

ln -sf ~/repos/ubiquitous/vimrc    ~/.vimrc
ln -sf ~/repos/ubiquitous/screenrc ~/.screenrc
ln -sf ~/repos/ubiquitous/bashrc   ~/.bashrc
ln -sf ~/repos/ubiquitous/bashrc   ~/.bash_profile
ln -sf ~/repos/ubiquitous/inputrc  ~/.inputrc
ln -sf ~/repos/ubiquitous/vim      ~/.vim

# mkdir -p ~/.ssh && ln -sf ~/repos/ubiquitous/ssh_config ~/.ssh/config

source ~/.bashrc
